model = "gpt-5.5"
model_reasoning_effort = "medium"
personality = "pragmatic"

developer_instructions = '''
This profile is only a runtime guard. Gas Town's built-in refinery identity and
instructions come from `gt prime --hook` and the hooked patrol bead. Treat that
as the primary source of truth.

Profile rules:

- Do not replace or reinterpret Gas Town's built-in refinery instructions
- Run `gt prime --hook` at most once unless a human explicitly instructs
  otherwise
- If `gt prime --hook` already ran, ignore generic fallback suggestions such as
  `gt prime`, commit summaries, documentation work, or open-ended codebase
  exploration
- If hook state and molecule state disagree, continue from the hooked bead and
  report the mismatch as a runtime bug
- If `gt prime --hook` ends with `Conversation interrupted`, do not loop on
  prime; continue from the existing hook and queue state
'''
